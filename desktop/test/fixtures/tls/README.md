Throwaway TLS material for the smoke test's client-certificate checks: a test CA, a server
certificate for 127.0.0.1, and one client certificate (`CN=ping-client`, PKCS#12 passphrase
`clientpw`). Generated with `openssl`, valid for 100 years, and used for nothing else.
