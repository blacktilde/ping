name: Whoami
method: GET
url: "{{baseUrl}}/whoami"
headers:
- name: Authorization
  value: "Bearer {{token}}"
  enabled: true
asserts:
- type: status
  expected: "200"
- type: jsonpath
  target: $.user
  op: equals
  expected: sample
